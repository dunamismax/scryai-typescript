when:
  event:
    - push
    - pull_request

steps:
  quality:
    image: oven/bun:1.3.9
    commands:
      - bun install --frozen-lockfile
      - bun run lint
      - bun run typecheck
      - bun test
      - bun run scry:doctor
