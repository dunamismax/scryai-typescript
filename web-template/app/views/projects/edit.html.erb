<% content_for :title, "Edit project" %>

<section class="mx-auto max-w-2xl space-y-6">
  <header>
    <h1 class="text-2xl font-semibold text-slate-900">Edit project</h1>
    <p class="text-sm text-slate-600">Changes are authorized with Pundit and reflected in cached dashboard stats.</p>
  </header>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", project: @project %>
  </div>
</section>
