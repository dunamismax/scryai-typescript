<% content_for :title, "New project" %>

<section class="mx-auto max-w-2xl space-y-6">
  <header>
    <h1 class="text-2xl font-semibold text-slate-900">New project</h1>
    <p class="text-sm text-slate-600">Projects are scoped to the current user and policy-protected.</p>
  </header>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", project: @project %>
  </div>
</section>
