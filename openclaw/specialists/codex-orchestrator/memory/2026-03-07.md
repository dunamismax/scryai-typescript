# 2026-03-07

- Stephen explicitly prefers repo-wide Codex swarms to run as real local Codex CLI sessions on macOS shell (`zsh` + PTY + `codex --full-auto` / `codex exec --full-auto`), not ACP/OpenClaw isolated sessions, unless ACP is explicitly requested.
- Settled repo rename decisions: `augur -> trade-desk-cli`, `go-web-server -> boring-go-web`, `oracle -> scryfall-discord-bot`, `imagingservices -> imaging-services-ops`, `grimoire -> scry-home`, `scripts -> pyforge`.
- Settled repo deletions: `homepage`, `images`, `sentinel`, `xray-chrome`, `imaging-services-website`, `dotfiles`.
- `dunamismax/REPOS.md` is intentionally deleted and no longer needed.
- For grimoire/scry-home, the long-term scheduler decision is to keep the existing nightly OpenClaw/grimoire flow; the imported interval LaunchAgent helper from dotfiles is legacy/manual-only.
- `imagingservices/website` is the chosen consolidation structure for the former `imaging-services-website` repo.
- The central `images` repo was used as a shared asset host for other repo READMEs/docs; referenced assets were migrated into consuming repos so `images` can be deleted.
- `dotfiles` was intentionally folded into `grimoire`/`scry-home` under the workstation/backup flow and no longer needs to remain a standalone repo.
- As of this session: local folders and both-host remotes were renamed for the six settled rename repos; Codeberg deletions were completed for the six settled delete repos; GitHub deletions for those six remained pending because the available GitHub auth lacked `delete_repo` scope and the managed GitHub browser session was not logged in.
- Stephen made a final keep decision for the active local portfolio/work set: `scry-home`, `dunamismax`, `boring-go-web`, `c-from-the-ground-up`, `scryfall-discord-bot`, `hello-world-from-hell`, `trade-desk-cli`, `Sawyer-Visual-Media`, and `openclaw`.
- Final chop-list repos to delete on both GitHub and Codeberg when convenient: `CallRift`, `elchess`, `imaging-services-ops`, `podwatch`, `pr-firefighter`, `pyforge`, `questlog`, and `rip`.
- Stephen explicitly has no sentimental attachment to old repos and prefers aggressive cleanup/consolidation; default posture can be blunt keep/delete decisions based on utility.
- Stephen currently wants Python preferred wherever practical for future repo work, but chose to keep `openclaw` as an upstream TypeScript contribution target rather than forcing a rewrite.
- `~/github/openclaw` was restored as the contribution clone with `origin=https://github.com/openclaw/openclaw.git`, `fork=git@github.com-dunamismax:dunamismax/openclaw.git`, `remote.pushDefault=fork`, `branch.main.remote=origin`, and local dependencies installed via `corepack pnpm install --frozen-lockfile`.
- The live OpenClaw install remains at `~/openclaw`; `~/github/openclaw` is the PR/contribution clone and should not be confused with the live runtime checkout.
- `scry-home` project inventory/docs were widened back from the temporary six-repo cutdown to the final nine-repo keeper set, and its project-reporting scripts now show `fork` remotes when present so the OpenClaw clone reports as PR-ready.
- `dunamismax` profile current-focus list was tightened to feature the strongest representative repos, with `openclaw` contributor work featured first; current featured set is `openclaw`, `scry-home`, `boring-go-web`, `c-from-the-ground-up`, and `scryfall-discord-bot`.
- `trade-desk-cli` is considered the strongest saved non-keeper and was restored locally because it is a real Python repo worth keeping and improving.
- `Sawyer-Visual-Media` is kept as a private business/ops repo; the public site was intentionally reduced to verified information only until real, permission-cleared portfolio assets and verified business-status details exist.
- Current local `~/github` git repos after cleanup/restores: `Sawyer-Visual-Media`, `boring-go-web`, `c-from-the-ground-up`, `dunamismax`, `hello-world-from-hell`, `openclaw`, `scry-home`, `scryfall-discord-bot`, and `trade-desk-cli`.
- GitHub repo deletion attempts were blocked by missing `delete_repo` scope on the available `gh` token; Codeberg CLI/browser auth was also not available for automated remote cleanup, so remote deletions remain a manual follow-up.
- Active OpenClaw contribution thread: PR `openclaw/openclaw#38977` is being worked in safe worktree `/tmp/oc-pr-38977` from contribution clone `~/github/openclaw` (never the live `~/openclaw` checkout).
- For PR `#38977`, commit `f9a97c849` (pushed to `fork/codex/discord-native-bound-session-routing`) resolved the review comments by: restoring `OPENCLAW_TEST_WORKERS` enforcement for UI/raw-arg test runs, using one fresh config snapshot end-to-end in Discord native command dispatch/model-picker flows, and preserving only explicit user auth-profile pins across `sessions.reset` while dropping auto-rotation metadata.
- Targeted verification already passed in `/tmp/oc-pr-38977` before base-sync: `src/discord/monitor/native-command.plugin-dispatch.test.ts` (5/5), `src/discord/monitor/native-command.model-picker.test.ts` (7/7), and `src/gateway/server.sessions.gateway-server-sessions-a.test.ts` (22/22).
- A follow-up `git merge origin/main` was started in `/tmp/oc-pr-38977` to clear GitHub conflict markers and is currently left mid-merge with unresolved conflicts in `scripts/test-parallel.mjs`, `src/discord/monitor/native-command.plugin-dispatch.test.ts`, `src/discord/monitor/native-command.test-helpers.ts`, and `src/discord/monitor/native-command.ts`; next pass should finish the merge, rerun the same targeted tests, commit the merge, and push.
