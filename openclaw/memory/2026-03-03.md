# 2026-03-03

## Session: Max Capability Audit + Git Install Fix

- Audited full OpenClaw capability surface: 51 bundled skills, config schema, plugin system, hooks, browser, ACP, webhooks, cron
- Cross-referenced with Stephen's tool stack: Notion, GitHub, Codeberg, Docker, VSCode, M365, macOS, Ghostty, RustDesk, Tailscale, Signal, Brave, LocalSend
- Config patch applied: browser (Brave), ACP (acpx/codex default), nested sub-agents (depth 2, 8 concurrent), web fetch (50K/30s)
- Discovered version drift: gateway running v2026.3.1 from stale npm install at `~/.openclaw/lib/`, while git repo at `~/openclaw` was v2026.3.3
- Fixed: nuked npm install (freed 671MB), symlinked lib → git repo. Binary was already symlinked correctly at `~/.local/bin/openclaw`
- Gateway now running v2026.3.3 from git, fully git-based
- Created CAPABILITY-GUIDE.md with tiered integration checklist
- Updated TOOLS.md, MEMORY.md with new state
- **Completed**: Brave Search API key configured, Notion "Scry" integration connected, ClawHub installed, Peekaboo installed (Accessibility granted, Screen Recording pending restart), summarize/tmux/whisper installed
- **Declined by Stephen**: Email integration (Himalaya) — both work and personal. Config + keychain entries removed.
- **Remaining**: Peekaboo Screen Recording (needs Ghostty restart), Notion page sharing (Stephen must share pages with "Scry" integration)

## Session: Agent Bench Buildout + Daily Maintenance Automation

- Peekaboo was fully removed by request (Homebrew package + local skill folder + local references); local Peekaboo automation is disabled unless reinstalled.
- Completed a broad repo/config security audit workflow with sub-agent support; no high-confidence live secret leaks found in working trees; CallRift `.gitignore` was hardened and pushed.
- Added persistent coding specialist `samantha` with dedicated workspace/agentDir and Codex-primary model (`openai-codex/gpt-5.3-codex`, fallback Opus).
- Added persistent specialist bench agents with dedicated workspaces + identity/runtime docs:
  - `sentinel`, `shipwright`, `caretaker`, `archivist`, `scout`, `operator`, `reviewer`, `builder-mobile`
  - model policy for these 8: Opus primary (`anthropic/claude-opus-4-6`) with Codex fallback.
- Updated main-agent governance docs to treat specialist delegation/maintenance as a living protocol (AGENTS/SOUL updates).
- Added `CLAUDE.md` to canonical workspace (`~/.openclaw/workspace/CLAUDE.md`) so bench health checks validate consistently.
- Added and validated daily cron maintenance jobs:
  - `healthcheck:agent-bench-daily` (3:20 AM ET): validates roster, core files, and model policy.
  - `healthcheck:docs-sync-daily` (3:30 AM ET): runs sync, auto-commits canonical drift, validates SOUL/AGENTS consistency and CLAUDE presence.
- Manually forced both new healthcheck jobs; fixed initial failure (`missing-file:main:CLAUDE.md`), then re-ran successfully.
- Verified reviewer/sentinel smoke tests completed successfully via Codex fallback after transient Opus timeouts.
- Added `codeberg` remote for `~/openclaw` and confirmed backup push path is active (`git push codeberg main` up-to-date).

## Session: Weekly Agent Bench Cron Repair

- Investigated failing cron job `optimization:weekly-agent-bench-review`; root cause was repeated LLM request timeouts on the initial oversized prompt.
- Patched the job payload to be timeboxed and deterministic (reduced scope, concise output contract), set model to Opus primary, and set `thinking: low` with `timeoutSeconds: 420`.
- Forced an immediate test run after patch; run completed successfully (~52s), delivered summary to Signal, and next scheduled run remains Monday 9:40 AM ET.
- Final state: job is healthy, no scheduler-level errors remaining for this task.

## Session: Model Policy Lock (No Cost-Downshift)

- Confirmed live agent configuration already complies with top-tier-only model policy: defaults are Opus primary with Codex fallback; Samantha is Codex primary with Opus fallback; all other specialists are Opus primary with Codex fallback.
- Updated weekly bench-review cron prompt with a hard model-policy constraint to prevent cost-driven downgrade recommendations.
- Added durable policy language to canonical `AGENTS.md` and `MEMORY.md`: optimize for capability/quality first; only use `anthropic/claude-opus-4-6` and `openai-codex/gpt-5.3-codex` unless Stephen explicitly overrides.
