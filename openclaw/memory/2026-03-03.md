# 2026-03-03

## Session 1 — Repo sweep
- All 16 local repos clean and synced
- Fixed: elchess Biome formatting, repo-monitor 11 a11y lint warnings
- All repos pass lint + typecheck

## Session 2 — Scry profile icon
- Added profile icon to grimoire README, committed + pushed

## Session 3 — Workspace reliability pass
- Filled `IDENTITY.md` and `USER.md` with current canonical identity/user context
- Added missing `BOOTSTRAP.md` so startup guidance is explicit and detectable
- Verified `SOUL.md`/`AGENTS.md` are byte-identical across workspace + grimoire copies
- Confirmed cron sync job exists but latest runs failed due temporary Anthropic overload

## Session 4 — Model + cron reliability decision
- Stephen requested switching default main model from Claude to Codex due Claude reliability issues
- Updated OpenClaw default model to `openai-codex/gpt-5.3-codex` in live config
- Updated nightly `sync-openclaw-workspace` cron payload to run on Codex with longer timeout (300s)

## Session 5 — Codex-only hard switch + docs pass
- Reconfirmed live default model is `openai-codex/gpt-5.3-codex`
- Removed Anthropic profile/model entries from live OpenClaw config for a Codex-only setup
- Updated workspace/grimoire MEMORY docs to reflect Codex as primary model
- Updated workflow wording from "Claude Code agents" to "Codex/ACP coding agents"
- Audited all `SOUL.md`, `AGENTS.md`, `CLAUDE.md`, and config docs in grimoire for stale model references

## Session 6 — Dual OAuth model toggle restored
- Re-enabled `anthropic:default` in OpenClaw auth profiles using OAuth (alongside `openai-codex:default`)
- Registered Claude model alias (`claude`) and kept Codex as primary default model
- Added Claude as fallback model so runtime can fail over when needed
- Verified the config now supports quick switching via `/model claude` and `/model codex`

## Session 7 — Model switching workflow aligned
- Stephen confirmed requirement: keep fast back-and-forth model switching between Codex and Claude (`/model codex` and `/model claude`) as part of normal workflow.
- OpenClaw config updated to dual OAuth profiles: `openai-codex:default` and `anthropic:default`.
- Agent model defaults now set to Codex primary with Claude fallback (`anthropic/claude-opus-4-6`) and Claude alias present in model map.
- Gateway restarted and config revalidated after the dual-profile change.
- Grimoire docs/memory synced and pushed after this adjustment (`76531d9`).
